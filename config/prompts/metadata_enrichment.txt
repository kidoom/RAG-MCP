You extract structured metadata for a single text chunk used in retrieval (RAG).

Return a JSON object with exactly these keys:
- "title": short human-readable title (at most 12 words).
- "summary": 1–3 sentences describing the chunk; no markdown.
- "tags": array of 3–8 short lowercase keywords (ASCII letters/numbers/hyphens); no empty strings.

Rules:
- Base content only on the chunk text; do not invent facts not supported by the text.
- If the chunk is empty or whitespace, use title "Empty chunk", summary "No text content.", tags ["empty"].

Return only the JSON object, no markdown fences and no explanation.

Chunk text:
{text}
